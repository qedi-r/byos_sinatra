<%= forme(@schedule_event, action: '/schedule_event') do |f|
    f.input :start_time, label: "Start time", type: :time
    f.input :end_time, label: "End Time", type: :time
    f.input :interruptible, type: :checkbox, label: "Interruptible"
    f.input :update_frequency, label: 'update frequency', type: :number

    f.input :plugins, label: 'Plugins (comma separated)'
    f.button 'Create Schedule'
  end
%>